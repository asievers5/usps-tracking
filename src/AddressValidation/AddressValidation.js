import React, { Fragment } from 'react';

const addressValidation = (props) => {
    return (
        <Fragment>
            <p>
                This is a placeholder for addressValidation
            </p>
        </Fragment>
    );
}

export default addressValidation;